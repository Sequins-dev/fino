import { Badge, Callout } from './mdx-components.tsx'

export const meta = { title: 'A small deck' }
export const components = { strong: Badge }
export const styles = '.fixture-deck { color: papayawhip; }'

# Hello, {props.name}

Markdown can be **strong** and [linked](https://example.com).

<Callout tone="warm">Custom component</Callout>

<Callout tone="cool" />

Inline <Badge>component</Badge> stays in prose.

---

## Second slide

- one
- two

| State | Owner |
| --- | --- |
| ~~draft~~ | Fino |

- [x] compiled

<Notes>Only the presenter sees this.</Notes>

```ts
const ready = true
```
