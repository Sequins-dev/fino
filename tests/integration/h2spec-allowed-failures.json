[
  {
    "id": "http2/3.5/Sends invalid connection preface",
    "reason": "invalid prefaces are rejected by closing the connection, with local raw H2 coverage verifying clean shutdown; h2spec requires a well-formed GOAWAY and can report EOF"
  },
  {
    "id": "http2/4.1/Sends a frame with undefined flag",
    "reason": "undefined frame flags are ignored locally and known flags are still honored, but h2spec can observe timing-sensitive final GOAWAY behavior in the TLS suite",
    "releaseAcceptableBecause": "Unknown flag bits have no application-facing behavior and local raw H2 coverage verifies the server ACKs the frame without emitting an error GOAWAY.",
    "localCoverage": "tests/net/http2.test.mts: ignores undefined frame flags and handles the known flags normally"
  },
  {
    "id": "http2/4.1/Sends a frame with reserved field bit",
    "reason": "reserved stream identifier bits are ignored locally, but h2spec can observe timing-sensitive TLS close behavior in the full section sequence",
    "releaseAcceptableBecause": "RFC 7540 requires the reserved bit to be ignored, and local raw H2 coverage verifies the request is accepted without an error GOAWAY.",
    "localCoverage": "tests/net/http2.test.mts: ignores the reserved stream id bit in frame headers"
  },
  {
    "id": "http2/4.2/Sends a large size DATA frame that exceeds the SETTINGS_MAX_FRAME_SIZE",
    "reason": "server detects the oversized DATA frame before nghttp2 consumes it, but h2spec can still observe EOF before the GOAWAY(FRAME_SIZE_ERROR) frame on this path"
  },
  {
    "id": "http2/4.2/Sends a DATA frame with 2^14 octets in length",
    "reason": "the exact default max-frame-size DATA path is accepted by local raw H2 coverage, but h2spec can still report a TLS-suite timeout during section sequencing"
  },
  {
    "id": "http2/5.1/half closed (remote): Sends a HEADERS frame",
    "reason": "half-closed HEADERS is deterministically RST_STREAMed with STREAM_CLOSED locally, but h2spec can still observe EOF before the reset frame in the full TLS suite"
  },
  {
    "id": "http2/5.1/half closed (remote): Sends a CONTINUATION frame",
    "reason": "half-closed CONTINUATION without an active header block is rejected locally, but h2spec can still observe timing-sensitive TLS close behavior in the full suite",
    "releaseAcceptableBecause": "RFC 9113 treats an unexpected CONTINUATION without an active header block as a connection-level protocol error, and local raw-wire coverage verifies that behavior.",
    "localCoverage": "tests/net/http2.test.mts: sends GOAWAY for representative CONTINUATION ordering errors"
  },
  {
    "id": "http2/5.1/closed: Sends a HEADERS frame",
    "reason": "closed-stream HEADERS is reset by the pre-recv validator locally, but h2spec still observes timing-sensitive close behavior in the full TLS suite"
  },
  {
    "id": "http2/5.1/closed: Sends a CONTINUATION frame",
    "reason": "closed-stream CONTINUATION remains timing-sensitive under h2spec; local regression coverage verifies deterministic error handling for representative CONTINUATION ordering cases"
  },
  {
    "id": "http2/5.1/closed: Sends a CONTINUATION frame after sending RST_STREAM frame",
    "reason": "closed-stream CONTINUATION after RST_STREAM is rejected by local raw H2 coverage, but h2spec can still observe EOF before the error frame in the full TLS suite"
  },
  {
    "id": "http2/5.5/Sends an unknown extension frame in the middle of a header block",
    "reason": "extension frames that interrupt a header block are rejected by local raw H2 coverage, but h2spec can still observe timing-sensitive TLS close behavior"
  },
  {
    "id": "http2/6.1/Sends a DATA frame with invalid pad length",
    "reason": "malformed padded DATA is rejected with GOAWAY(PROTOCOL_ERROR) in local raw H2 coverage, but h2spec can still observe EOF before the GOAWAY frame"
  },
  {
    "id": "http2/6.5/Sends a SETTINGS frame with ACK flag and payload",
    "reason": "SETTINGS ACK frames with payload are rejected with GOAWAY(FRAME_SIZE_ERROR) in local raw H2 coverage, but h2spec can still observe EOF before the GOAWAY frame"
  },
  {
    "id": "http2/6.5.3/Sends a SETTINGS frame without ACK flag",
    "reason": "follow-up non-ACK SETTINGS frames are ACKed in local raw H2 coverage, but h2spec can still time out waiting for the ACK during full TLS section sequencing"
  },
  {
    "id": "http2/6.5.3/Sends multiple values of SETTINGS_INITIAL_WINDOW_SIZE",
    "reason": "duplicate SETTINGS_INITIAL_WINDOW_SIZE entries are ACKed without GOAWAY in local raw H2 coverage, but h2spec can still time out waiting for the ACK during full TLS section sequencing",
    "releaseAcceptableBecause": "RFC 7540 settings are processed in appearance order, and local raw-wire coverage verifies the server accepts duplicate setting identifiers without treating them as protocol errors.",
    "localCoverage": "tests/net/http2.test.mts: ACKs duplicate SETTINGS_INITIAL_WINDOW_SIZE entries"
  },
  {
    "id": "http2/6.7/Sends a PING frame",
    "reason": "server responds to normal PING frames through nghttp2, but h2spec can still time out waiting for PING ACK during full TLS section sequencing"
  },
  {
    "id": "http2/6.7/Sends a PING frame with ACK",
    "reason": "client PING ACK frames are ignored locally without an error GOAWAY, but h2spec can still report timeout/EOF during full TLS section sequencing",
    "releaseAcceptableBecause": "RFC 7540 requires endpoints to ignore PING ACK frames; local raw-wire coverage verifies no PING response and no protocol error.",
    "localCoverage": "tests/net/http2.test.mts: closes cleanly for invalid PING length and ignores client PING ACK"
  },
  {
    "id": "http2/6.10/Sends a CONTINUATION frame followed by any frame other than CONTINUATION",
    "reason": "the validator rejects interrupted header blocks before feeding them to nghttp2, but h2spec can still observe EOF before GOAWAY(PROTOCOL_ERROR)"
  },
  {
    "id": "http2/6.10/Sends a CONTINUATION frame preceded by a DATA frame",
    "reason": "stray CONTINUATION after DATA is rejected with GOAWAY(PROTOCOL_ERROR) locally, but h2spec can still observe EOF before the GOAWAY frame in the full TLS suite",
    "releaseAcceptableBecause": "Local raw-wire coverage verifies the connection-level protocol error and no second request is dispatched.",
    "localCoverage": "tests/net/http2.test.mts: sends GOAWAY for representative CONTINUATION ordering errors"
  },
  {
    "id": "http2/8.1.2.1/Sends a HEADERS frame that contains the pseudo-header field defined for response",
    "reason": "request HEADERS with response-only pseudo-headers are deterministically RST_STREAMed with PROTOCOL_ERROR in local raw H2 coverage, but h2spec can still report the TLS suite case as failing"
  },
  {
    "id": "http2/8.1.2.1/Sends a HEADERS frame that contains a pseudo-header field as trailers",
    "reason": "trailers containing pseudo-headers are deterministically RST_STREAMed with PROTOCOL_ERROR in local raw H2 coverage, but h2spec can still report the TLS suite case as failing",
    "releaseAcceptableBecause": "Local raw-wire coverage verifies the invalid trailer block is rejected before any successful request completion is exposed to the handler.",
    "localCoverage": "tests/net/http2.test.mts: RST_STREAMs trailers containing pseudo-headers"
  },
  {
    "id": "http2/8.1.2/Sends a HEADERS frame that contains the header field name in uppercase letters",
    "reason": "uppercase request header field names are deterministically RST_STREAMed with PROTOCOL_ERROR in local raw H2 coverage, but h2spec can still report the TLS suite case as failing",
    "releaseAcceptableBecause": "Local raw-wire coverage verifies the stream-level protocol error before request dispatch.",
    "localCoverage": "tests/net/http2.test.mts: RST_STREAMs request HEADERS with uppercase field names"
  },
  {
    "id": "http2/8.1.2.1/Sends a HEADERS frame that contains a pseudo-header field that appears in a header block after a regular header field",
    "reason": "request HEADERS with pseudo-headers after regular headers are deterministically RST_STREAMed with PROTOCOL_ERROR in local raw H2 coverage, but h2spec can still report the TLS suite case as failing",
    "releaseAcceptableBecause": "Local raw-wire coverage verifies the stream-level protocol error before request dispatch.",
    "localCoverage": "tests/net/http2.test.mts: RST_STREAMs request HEADERS with pseudo-header after a regular header"
  },
  {
    "id": "http2/8.1.2.3/Sends a HEADERS frame with empty \":path\" pseudo-header field",
    "reason": "empty :path request HEADERS are deterministically RST_STREAMed with PROTOCOL_ERROR locally, but h2spec can still report the TLS suite case as failing",
    "releaseAcceptableBecause": "Local raw-wire coverage verifies the stream-level protocol error before request dispatch.",
    "localCoverage": "tests/net/http2.test.mts: RST_STREAMs request HEADERS with missing or empty :path"
  },
  {
    "id": "http2/8.1.2.3/Sends a HEADERS frame with duplicated \":scheme\" pseudo-header field",
    "reason": "duplicate :scheme request HEADERS are deterministically RST_STREAMed with PROTOCOL_ERROR locally, but h2spec can still report the TLS suite case as failing",
    "releaseAcceptableBecause": "Local raw-wire coverage verifies the stream-level protocol error before request dispatch.",
    "localCoverage": "tests/net/http2.test.mts: RST_STREAMs request HEADERS with duplicate :scheme"
  },
  {
    "id": "http2/8.1.2.3/Sends a HEADERS frame that omits \":path\" pseudo-header field",
    "reason": "missing :path request HEADERS are deterministically RST_STREAMed with PROTOCOL_ERROR locally, but h2spec can still report the TLS suite case as failing",
    "releaseAcceptableBecause": "Local raw-wire coverage verifies the stream-level protocol error before request dispatch.",
    "localCoverage": "tests/net/http2.test.mts: RST_STREAMs request HEADERS with missing or empty :path"
  },
  {
    "id": "http2/8.1/Sends a second HEADERS frame without the END_STREAM flag",
    "reason": "a second non-trailer request HEADERS block on an open stream is RST_STREAMed with PROTOCOL_ERROR locally, but h2spec can still report the TLS suite case as failing",
    "releaseAcceptableBecause": "Local raw-wire coverage verifies the stream-level error and the server does not dispatch a second request on the stream.",
    "localCoverage": "tests/net/http2.test.mts: RST_STREAMs a second request HEADERS frame on an open stream"
  },
  {
    "id": "http2/8.1.2.6/Sends a HEADERS frame with the \"content-length\" header field which does not equal the DATA frame payload length",
    "reason": "mismatched content-length is RST_STREAMed with PROTOCOL_ERROR locally after body completion, but h2spec can still report the TLS suite case as failing",
    "releaseAcceptableBecause": "The server preserves progressive request-body processing allowed by RFC 9113, but malformed bodies deterministically error the request body, reset the stream with PROTOCOL_ERROR, and never produce a successful application response.",
    "localCoverage": "tests/net/http2.test.mts: RST_STREAMs malformed, short, and overlong content-length request bodies without response HEADERS/DATA"
  }
]
